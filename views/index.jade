extends layout

block title
  | - Welcome

//-block topspacer

block app
  .page
    .container
      h1 Welcome

      each doc in docs.results
        p=doc.fragments['post.title'].value[0].text + ' - ' + (doc.fragments['post.postDate'] ? doc.fragments['post.postDate'].value : 'NO DATA')

//-block scripts
